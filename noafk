local a=Instance.new("ScreenGui")
local b=Instance.new("Frame")
local c=Instance.new("TextLabel")
local d=Instance.new("TextButton")
local e=false
local f=false
a.Parent=game:GetService("CoreGui")
a.Name="AntiAFK_GUI"
b.Size=UDim2.new(0,250,0,100)
b.Position=UDim2.new(0.5,-125,0.5,-50)
b.BackgroundColor3=Color3.fromRGB(30,30,30)
b.BorderSizePixel=0
b.Draggable=true
b.Active=true
b.Parent=a
c.Text="Anti-AFK Script"
c.Size=UDim2.new(1,0,0,30)
c.BackgroundTransparency=1
c.TextColor3=Color3.fromRGB(255,255,255)
c.Font=Enum.Font.SourceSansBold
c.TextSize=20
c.Parent=b
d.Size=UDim2.new(0.8,0,0,40)
d.Position=UDim2.new(0.1,0,0.5,-20)
d.BackgroundColor3=Color3.fromRGB(70,130,180)
d.Text="Enable Anti-AFK"
d.TextColor3=Color3.fromRGB(255,255,255)
d.Font=Enum.Font.SourceSans
d.TextSize=18
d.Parent=b
local g
local function h()
	if f then
		if g then g:Disconnect() end
		d.Text="Enable Anti-AFK"
		f=false
	else
		g=game:GetService("Players").LocalPlayer.Idled:Connect(function()
			local i=game:GetService("VirtualUser")
			i:Button2Down(Vector2.new(0,0),workspace.CurrentCamera.CFrame)
			wait(1)
			i:Button2Up(Vector2.new(0,0),workspace.CurrentCamera.CFrame)
		end)
		d.Text="Disable Anti-AFK"
		f=true
	end
end
d.MouseButton1Click:Connect(h)
